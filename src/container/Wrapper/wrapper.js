import React from 'react';
// import Logo from '../Header/logo'

const wrapper = (props) => 
    <div>
        <header>
        </header>
        {props.children}
        <footer>
        </footer>
    </div>


export default wrapper;